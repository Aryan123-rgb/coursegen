services:
  db:
    image: postgres:16
    container_name: coursegen-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: coursegen
    ports:
      - "5432:5432"
